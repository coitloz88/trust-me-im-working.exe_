[
  "const result = await fetchUser(userId)",
  "function validateInput(data: unknown): boolean {",
  "  return typeof data === 'string' && data.length > 0",
  "export default async function handler(req, res) {",
  "interface UserProfile {",
  "  id: string",
  "  name: string",
  "  createdAt: Date",
  "const [loading, setLoading] = useState(false)",
  "useEffect(() => {",
  "  document.title = 'Working hard...'",
  "}, [])",
  "// TODO: refactor this later",
  "// FIXME: this edge case is not handled yet",
  "// Do not touch. It works somehow.",
  "// I have no idea what this does",
  "// This will be fixed in the next sprint",
  "// Temporary workaround — see issue #4021",
  "return res.status(200).json({ ok: true })",
  "throw new Error('Unexpected state reached')",
  "const config = JSON.parse(process.env.CONFIG ?? '{}')",
  "type Result<T> = { data: T } | { error: Error }",
  "class AuthService implements IAuthService {",
  "  private readonly db: Database",
  "await Promise.all(tasks.map(t => t.run()))",
  "if (process.env.NODE_ENV !== 'production') {",
  "const schema = z.object({ id: z.string().uuid() })",
  "const start = performance.now()",
  "logger.info('Request received', { path: req.url })",
  "const MAX_RETRIES = 3",
  "let attempts = 0",
  "while (attempts < MAX_RETRIES) {",
  "export const sleep = (ms: number) => new Promise(r => setTimeout(r, ms))",
  "if (!user) return redirect('/login')",
  "const token = crypto.randomUUID()",
  "const { data, error } = await supabase.from('users').select()",
  "return NextResponse.redirect(new URL('/dashboard', req.url))",
  "const hash = await bcrypt.hash(password, 10)",
  "type Awaited<T> = T extends Promise<infer R> ? R : T",
  "export function debounce<T extends (...args: unknown[]) => void>(fn: T, ms: number) {",
  "const emit = useCallback(() => onUpdate(value), [onUpdate, value])",
  "ref.current?.scrollIntoView({ behavior: 'smooth' })",
  "const [state, dispatch] = useReducer(reducer, initialState)",
  "return <Suspense fallback={<Spinner />}><LazyComponent /></Suspense>",
  "const signal = new AbortController().signal",
  "fetch(url, { signal }).then(r => r.json())",
  "Object.entries(config).forEach(([k, v]) => process.env[k] = String(v))",
  "// performance.mark('render-start')",
  "export type { UserProfile, AuthConfig } from './types'",
  "const middleware = [logger, rateLimiter, authenticate]",
  "router.get('/api/users', authenticate, async (req, res) => {",
  "const span = tracer.startSpan('db.query')",
  "  span.setAttribute('db.table', 'users')",
  "db.transaction(async (trx) => {",
  "  await trx('users').insert(payload)",
  "  await trx('audit_log').insert({ action: 'create' })",
  "const observer = new IntersectionObserver(onVisible, { threshold: 0.5 })",
  "requestIdleCallback(() => prefetch(nextRoute))",
  "const worker = new Worker('./heavy-task.js', { type: 'module' })",
  "sessionStorage.setItem('draft', JSON.stringify(form))"
]
